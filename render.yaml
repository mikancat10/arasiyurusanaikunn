services:
  - type: worker
    name: security-fortress-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    disk:
      name: bot-storage
      mountPath: /data
      sizeGB: 1
